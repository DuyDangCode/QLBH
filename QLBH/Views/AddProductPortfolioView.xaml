<Window x:Class="QLBH.Views.AddProductPortfolioView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QLBH.Views"
        xmlns:viewModel="clr-namespace:QLBH.ViewModels"
        mc:Ignorable="d"
        Title="Thêm danh mục sản phẩm" Height="450" Width="800">

    <Window.DataContext>
        <viewModel:AddProductPortfolioViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock>Tên sản phẩm: </TextBlock>
            <TextBox></TextBox>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <TextBlock>Đơn vị:</TextBlock>
            <ComboBox ItemsSource="{Binding units}"></ComboBox>
        </StackPanel>
        <Image></Image>
    </Grid>
</Window>
